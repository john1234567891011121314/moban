## 数论
任意两个自然数互质的概率为$\frac{6}{\pi^2}$​
$$
\sum_{i=1}^{N} \phi(i) \approx \frac{3 * N ^ 2}{\pi^2}
$$

### 生成法雷序列（互素对）

```c++
void dfs(int a1, int b1, int a2, int b2)  //a1 / b1 , a2 / b2
{
    if (b1 + b2 > n) return;
    dfs(a1, b1, a1 + a2, b1 + b2);  
    printf("%d/%d ", a1 + a2, b1 + b2);
    dfs(a1 + a2, b1 + b2, a2, b2);  
}
//dfs(0,1,1,1) x < y
```



### 生成勾股数

```c++
for (int i = 1; i <= limit; ++i)
{
    for (int j = i + 1; j <= limit; j += 2)
    {
        if (std::gcd(i, j) != 1) continue;
        int x = j * j - i * i, y = 2 * j * i, z = j * j + i * i;
        int tmp1 = x, tmp2 = y, tmp3 = z;
        if (tmp1 > tmp2) swap(tmp1, tmp2);
        while (tmp3 <= up2)
        {
            a[tmp3].push_back({tmp1, tmp2});
            tmp1 += x, tmp2 += y, tmp3 += z;
        }
    }
}
```



```c++
void gen(int a, int b, int c)
{
    if (a > 300'000 || b > 300'000 || c > 300'000) return;
    // cout << a << " " << b << " " << c << endl;
    ggs[c].push_back({a, b});
    gen(a + 2 * b + 2 * c, 2 * a + b + 2 * c, 2 * a + 2 * b + 3 * c);
    gen(-a + 2 * b + 2 * c, -2 * a + b + 2 * c, -2 * a + 2 * b + 3 * c);
    gen(a - 2 * b + 2 * c, 2 * a - b + 2 * c, 2 * a - 2 * b + 3 * c);
}
//gen(3,4,5)
```

